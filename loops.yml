---
 - hosts: 172.31.5.245
   become: yes
   tasks:
     - name: Install Multiple tasks
       apt:
        name:"{{ item }}" 
        state: present
       loops:
       - git
       - tree
       - wget 